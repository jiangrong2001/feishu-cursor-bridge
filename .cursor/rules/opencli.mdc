---
description: 可选第三方工具 OpenCLI（与飞书桥接无关）；用户提到 opencli、小红书发帖、多站点 CLI 时可查阅本规则并执行终端命令。
globs:
alwaysApply: false
---

# OpenCLI（可选，与 feishu-cursor-bridge 无关）

> **说明**：本仓库核心是 **飞书 ↔ Cursor Agent CLI 桥接**。**OpenCLI** 为独立项目 [jackwener/opencli](https://github.com/jackwener/opencli)，与桥接 **无依赖、无代码集成**；本 `.mdc` 仅**随仓库存档**，方便在同一工作区用 Cursor 时检索用法。使用与否不影响 `npm start` 与飞书长连接。

官方文档（中文）：[README.zh-CN](https://github.com/jackwener/opencli/blob/main/README.zh-CN.md)

## 安装（全局，用户本机执行）

```bash
npm install -g @jackwener/opencli@latest
```

需 **Node.js ≥ 20**。

## 前置条件（浏览器类命令）

- **Chrome** 运行中，并安装 **Browser Bridge** 扩展（见 OpenCLI 官方 README「Browser Bridge 扩展配置」）。
- 目标站点（如小红书）在 Chrome 中**已登录**。
- 排错：`opencli doctor`、`opencli daemon status`。

## 发现命令

```bash
opencli list
opencli list -f yaml
```

## 小红书发帖（示例）

```bash
opencli xiaohongshu publish "正文内容" --title "标题二十字内" --images "/path/a.jpg,/path/b.png" --topics "话题1,话题2"
```

- 草稿：`--draft true`；图片最多 9 张；`opencli xiaohongshu publish -h` 查看全部参数。

## 给 Agent 的建议

- 结构化输出加 `-f json`。
- 子命令以 `opencli list` 与 `--help` 为准，勿臆造。
- 可选：`opencli register lark-cli` 后用 `opencli lark-cli ...` 透传（与桥接用的 `lark-cli` 仍是同一二进制，非本仓库功能）。

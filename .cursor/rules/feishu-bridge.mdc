---
description: 飞书桥接写入的 inbox 日志与排错（本项目仅 Cursor Agent 全自动）
globs: inbox/LATEST.md,inbox/LATEST.json
---

# 飞书 → Cursor 桥接

- 运行 `npm start` 且 `.env` 中 **`CURSOR_AGENT_AUTO=1`** 时，桥接会 **spawn Cursor Agent CLI**，无需在 IDE 里手动「处理队列」。
- `inbox/LATEST.md` / `LATEST.json` 是**日志**：便于对照 `chat_id`、用户原文；若全自动异常，可据此用 `lark-cli` 手动发一条验证权限。

手动发飞书（示例）：

`lark-cli im +messages-send --as bot --chat-id "<chat_id>" --text "<正文>"`

勿泄露 appSecret、token。说明见根目录 `README.zh.md`。
